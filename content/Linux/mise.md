Mise (pronounced "meez") is a development environment setup tool. It can install and manage dev tools/runtimes like node, python, or php.

## Runtimes

### PHP

Debian dependencies

```
build-essentetials cpp unzip locate autoconf bison re2c pkg-config libxml2-dev libsqlite3-dev zlib1g-dev libcurl4-openssl-dev libgd-dev libonig-dev libpq-dev libreadline-dev libzip-dev
```
